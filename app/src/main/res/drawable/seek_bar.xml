<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape
            android:shape="line">
            <stroke
                android:color="#FBCEB2"
                android:width="@dimen/_6sdp"/>
        </shape>
        <corners
            android:radius="@dimen/_10sdp"/>

    </item>
    <item android:id="@android:id/secondaryProgress">
        <clip>
            <shape
                android:shape="line">
                <stroke
                    android:color="@color/seek_bar_secondary_progress"
                    android:width="@dimen/_4sdp"/>
            </shape>


        </clip>
    </item>

    <item android:id="@android:id/progress">
        <clip>
            <shape
                android:shape="line">
                <stroke
                    android:color="@color/calHeaderT"
                    android:width="@dimen/_6sdp"/>

                <corners
                    android:radius="@dimen/_10sdp"/>
            </shape>
        </clip>
    </item>

</layer-list>